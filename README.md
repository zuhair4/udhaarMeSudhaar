# Law Firm React App (Scaffold)

This is a minimal Vite + React scaffold for a law firm website.

Quick start:

```bash
npm install
npm run dev
```

Files added:
- `package.json`, `vite.config.js`, `index.html`
- `src/main.jsx`, `src/App.jsx`, `src/index.css`
- `src/components/Header.jsx`, `src/components/Home.jsx`

Next steps:
- Tell me the sections/pages you want and I'll add routes and components.
